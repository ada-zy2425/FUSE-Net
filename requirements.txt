# Direct dependency versions validated on Python 3.12 / CPU.
# These are not the recovered environment of the historical paper experiments.
torch==2.5.1
transformers==4.46.3
numpy==2.3.5
scikit-learn==1.8.0
# CMU-MultimodalSDK is needed only for raw MOSI/MOSEI preprocessing.
