# Third-party source and dependencies

The uploaded `FUSENet/utils/functions.py` explicitly attributes portions of its utilities to:

- `fungtion/DSN`, `functions.py`: https://github.com/fungtion/DSN/blob/master/functions.py
- `wzell/cmd`, `models/domain_regularizer.py`: https://github.com/wzell/cmd/blob/master/models/domain_regularizer.py

Those comments are preserved in the source. This file records the attributions found in the upload; it is not a complete dependency or license audit, and it does not grant additional permissions.

The implementation also depends on PyTorch, Hugging Face Transformers and model assets, NumPy, scikit-learn, tqdm, and the CMU Multimodal SDK. Datasets, model weights, and the SDK are obtained separately under their respective terms.

No project-wide license was supplied. The private snapshot does not introduce an MIT, Apache, or other blanket license. Before a public release, select the project license and confirm the provenance and notice requirements for retained third-party source and the accompanying manuscript.
